# 全球零售运营数据分析与可视化决策看板 — 项目架构说明

**项目作者**：![1940-sy-1940](https://github.com/1940-sy-1940.png?size=40) [1940-sy-1940](https://github.com/1940-sy-1940)

---

## 一、项目整体概览

### 1. 核心定位
基于 Global Superstore 全球零售订单数据，从区域销售、品类销量、客户复购、物流时效四个维度展开多维度分析，自动定位低效区域、低销品类、高延迟渠道等核心业务问题，输出交互式图表与分析报告，支撑运营决策。

### 2. 技术栈总览
| 维度 | 选型 |
|------|------|
| 开发语言 | Python 3.10 |
| 数据分析 | Pandas、NumPy |
| 可视化 | Matplotlib |
| 数据格式 | Excel (.xlsx) / CSV |
| 架构模式 | Cookiecutter Data Science |
| 配套工具 | Power Query (ETL) + Power BI (看板) |
| 交互看板 | Streamlit |

### 3. 架构分层

```
┌──────────────────────────────────────────────┐
│ main.py (CLI)  │  app.py + pages/ (Streamlit) │  ← 交互层
├──────────────────────────────────────────────┤
│ src/config.py                                │  ← 配置层
├──────────┬──────────┬────────────────────────┤
│ data/    │ analysis/│ visualization/          │  ← 计算层
│ make_    │ dimen-   │ visualize.py            │
│ dataset  │ sions.py │                         │
├──────────┴──────────┴────────────────────────┤
│ outputs/data/ outputs/reports/  figures/      │  ← 产出层
│ data/processed/ (缓存)                        │  ← 缓存层
├──────────────────────────────────────────────┤
│ src/powerbi/ (Power BI Push API)             │  ← 集成层
└──────────────────────────────────────────────┘
```

---

## 二、顶层核心目录结构

```
全球零售运营数据分析与可视化决策看板/
├── main.py                            # CLI (analyze / report / push)
├── app.py                             # Streamlit 看板入口
├── setup.py                           # 包配置
├── requirements.txt
├── README.md
├── .gitignore
│
├── data/
│   ├── raw/                           # 原始数据，只读
│   │   ├── Global_Superstore2.xlsx
│   │   └── Global_Superstore2.csv
│   └── processed/                     # 清洗后缓存
│       └── cleaned_data.csv
│
├── pages/                             # Streamlit 多页看板
│   ├── 1_销售总览.py
│   ├── 2_品类分析.py
│   ├── 3_客户复购.py
│   └── 4_物流时效.py
│
├── src/
│   ├── config.py                      # 路径 / 阈值 / 图表参数
│   ├── data/
│   │   └── make_dataset.py            # load_raw + preprocess + make_dataset
│   ├── analysis/
│   │   └── dimensions.py              # 四维分析 + 问题定位 + 落盘
│   ├── visualization/
│   │   └── visualize.py               # 8 张 PNG 图表
│   ├── dashboard/
│   │   ├── loader.py                  # Streamlit 数据加载与缓存
│   │   └── styles.py                  # 全局配色 / CSS
│   └── powerbi/
│       ├── auth.py                    # Power BI 认证
│       └── push_data.py               # Push API 推送
│
├── docs/
│   └── power_bi_guide.md              # Power BI 接入指南
│
├── outputs/
│   ├── data/                          # 分析结果 CSV（二次执行可复用）
│   ├── reports/                       # analysis_report.md
│   └── figures/                       # 01~08_*.png
│
├── tests/
│   └── test_pipeline.py               # 端到端测试
│
├── .streamlit/
│   └── config.toml                    # Streamlit 配置
│
└── pbi_config.example.json            # Power BI 配置模板
```

---

## 三、模块与文件职责详解

### 3.1 `src/config.py`
- **核心职责**：路径 / 分析阈值 / 图表样式集中管理。
- **关键能力**：`DATA_RAW_DIR`、`CLEANED_DATA` 数据路径；`LOW_PERFORMANCE_QUANTILE=0.25` 低效标记分位；`REPURCHASE_THRESHOLD=2` 复购阈值；`TOP_N_CHARTS=15`、`CHART_DPI=150` 图表参数。

### 3.2 `src/data/make_dataset.py`
- **核心职责**：读取 Excel/CSV → 列名规范化 → 日期解析 → ShippingDays/ProfitRate/Order_YM 衍生 → 缺失值填充 → IQR 截断 → 写入 `data/processed/cleaned_data.csv`。
- **闭环机制**：`make_dataset()` 检测缓存存在则直接加载，跳过清洗。

### 3.3 `src/analysis/dimensions.py`
- **核心职责**：四维分析聚合 + 低效标记 + 问题定位 + 结果落盘。
- **关键能力**：
  - `analyze_regional_sales()` → Market/Region/Country 三层粒度
  - `analyze_category_sales()` → Category/Sub-Category
  - `analyze_customer_repurchase()` → 客户分层（高频/中频/低频/一次性） + 复购率
  - `analyze_logistics()` → Ship Mode + Order Priority + 延迟订单 (P95)
  - `identify_business_issues()` → 低效区域/低销品类/高延迟渠道
  - `save_analysis_results()` → outputs/data/*.csv（布尔列转中文标签）

### 3.4 `src/visualization/visualize.py`
- **核心职责**：8 张 PNG 图表（区域×2、品类×2、客户×2、物流×2）。
- **关键能力**：`generate_all_charts()` 批量生成。

### 3.5 `src/dashboard/`
- **loader.py**：`@st.cache_data(ttl=3600)` 缓存加载四维分析结果，供各页面复用。
- **styles.py**：全局配色 / CSS / 图表调色板，统一看板视觉。

### 3.6 `app.py`（Streamlit 看板入口）
- **核心职责**：品牌侧边栏、全局筛选（市场/品类/运输方式）、`st.navigation` 组织 4 个页面。
- **关键能力**：`needs_analyze()` 检查数据是否就绪，引导执行 `main.py analyze` 生成结果后再进入看板。

### 3.7 `pages/`（看板页面）
- **1_销售总览.py**：KPI 卡片、市场对比图、低效市场预警、国家地图
- **2_品类分析.py**：品类销量与利润、子品类排行
- **3_客户复购.py**：客户分层结构、复购率概览
- **4_物流时效.py**：运输方式对比、订单延迟分布

### 3.8 `src/powerbi/`（Power BI 集成）
- **auth.py**：Azure AD 认证，获取 Power BI API 访问令牌
- **push_data.py**：dtype→Power BI 类型映射、分批推送（≤10000 行）、幂等建集

---

## 四、核心运行链路

### 4.1 命令模式

```
python main.py analyze → 清洗 → 四维分析 → CSV + 图表 + 报告
python main.py report  → 加载 outputs/data/*.csv → 图表 + 报告
python main.py push    → 加载 outputs/data/*.csv → 推送 Power BI
streamlit run app.py   → 启动交互看板
```

### 4.2 analyze 链路

```
Step 1: make_dataset()                → 优先读缓存，否则清洗 → data/processed/
Step 2: analyze_regional_sales()      → Market/Region/Country
Step 3: analyze_category_sales()      → Category/Sub-Category
Step 4: analyze_customer_repurchase() → 客户分层 + 复购率
Step 5: analyze_logistics()           → Ship Mode + 延迟分析
Step 6: save_analysis_results()       → outputs/data/*.csv
        generate_all_charts()         → outputs/figures/*.png
        identify_business_issues()    → outputs/reports/analysis_report.md
```

### 4.3 Streamlit 看板链路

```
streamlit run app.py
  → needs_analyze() 检查数据
  → st.navigation 路由到 pages/
  → loader.py 加载 outputs/data/*.csv
  → 渲染图表 / 地图 / 预警
```

---

## 五、配置与环境体系

### 5.1 核心配置

| 参数 | 含义 | 默认值 |
|------|------|--------|
| `LOW_PERFORMANCE_QUANTILE` | 低效标记分位 | 0.25 |
| `REPURCHASE_THRESHOLD` | 复购判定阈值 | 2 |
| `TOP_N_CHARTS` | 图表 TOP-N | 15 |
| `CHART_DPI` | 图表分辨率 | 150 |

### 5.2 Power BI 联动

1. 运行 `python main.py analyze` 生成 `outputs/data/*.csv`
2. 在 Power BI Desktop 中通过"获取数据 → CSV"导入
3. `Is_LowPerformance` / `Is_LowSales` 列可用于条件格式/钻取
4. 或运行 `python main.py push` 通过 Push API 直接推送至云端数据集

### 5.3 Streamlit 配置

`.streamlit/config.toml` 控制看板主题与运行参数。

---

## 六、构建与部署

```bash
pip install -r requirements.txt
python main.py analyze
streamlit run app.py
```

---

## 七、扩展指引

1. 新增分析维度：在 `dimensions.py` 中添加 `analyze_xxx(df)` 函数
2. 新增图表：在 `visualize.py` 中添加并注册到 `generate_all_charts()`
3. 新增看板页面：在 `pages/` 下新建 `N_标题.py` 即可自动接入导航
4. 数据预处理逻辑：追加到 `make_dataset.py`
5. 新增 Power BI 推送：在 `push_data.py` 中扩展数据集
